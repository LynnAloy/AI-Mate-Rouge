using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShowTimeScale : MonoBehaviour
{
    // Start is called before the first frame update

    private void Awake()
    {
        Debug.Log($"Show TimeScale: {Time.timeScale}");
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
